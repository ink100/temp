<UserControl x:Class="HRB.Platform.Client.WPF.PaymentAppModule.Views.NotificationConsentDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodels="clr-namespace:HRB.Platform.Client.WPF.PaymentAppModule.Core.ViewModels;assembly=HRB.Platform.Client.WPF.PaymentAppModule.Core"
             xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             Width="900"
             Height="700"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <!--  自定义布尔值转可见性转换器  -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" 
                                                 xmlns:converters="clr-namespace:HRB.Platform.Client.WPF.PaymentAppModule.Core.Converters;assembly=HRB.Platform.Client.WPF.PaymentAppModule.Core" />

        <!--  按钮样式  -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="30,12" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Padding="30" Background="White" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
                <RowDefinition Height="30" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  标题  -->
            <TextBlock Grid.Row="0"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333333"
                       Text="{Binding Title}"
                       TextAlignment="Center" />

            <!--  知情同意书内容（Markdown渲染）  -->
            <Border Grid.Row="2"
                    Background="White"
                    BorderBrush="#CCCCCC"
                    BorderThickness="1"
                    CornerRadius="4">
                <markdig:MarkdownViewer Markdown="{Binding ConsentContent}"
                                        Padding="20"
                                         />
            </Border>

            <!--  提示文字  -->
            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <!--  只读模式提示  -->
                <TextBlock FontSize="14"
                           Foreground="#0078D7"
                           Text="请仔细阅读以上条款内容。"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Visibility="{Binding IsReadOnlyMode, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!--  确认模式提示  -->
                <TextBlock FontSize="14"
                           Foreground="#DC3545"
                           Text="请仔细阅读以上条款，点击 同意 表示您已充分理解并接受所有条款。"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Visibility="{Binding IsReadOnlyMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
            </StackPanel>

            <!--  按钮  -->
            <StackPanel Grid.Row="4"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                <!--  只读模式：只显示确认按钮  -->
                <Button Background="#0078D7"
                        Command="{Binding AgreeCommand}"
                        Content="确认"
                        Style="{StaticResource DialogButtonStyle}"
                        Visibility="{Binding IsReadOnlyMode, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!--  确认模式：显示同意和拒绝按钮  -->
                <Button Background="#4CAF50"
                        Command="{Binding AgreeCommand}"
                        Content="同意"
                        Style="{StaticResource DialogButtonStyle}"
                        Visibility="{Binding IsReadOnlyMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                <Button Margin="20,0,0,0"
                        Background="#DC3545"
                        Command="{Binding DeclineCommand}"
                        Content="拒绝"
                        Style="{StaticResource DialogButtonStyle}"
                        Visibility="{Binding IsReadOnlyMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
