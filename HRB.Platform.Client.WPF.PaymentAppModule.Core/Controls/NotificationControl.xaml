<UserControl x:Class="HRB.Platform.Client.WPF.PaymentAppModule.Core.Controls.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border x:Name="NotificationBorder"
            MinWidth="300"
            MinHeight="60"
            MaxWidth="500"
            Margin="0,0,0,10"
            Padding="20,15"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="#4CAF50"
            BorderThickness="0"
            CornerRadius="8"
            Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform Y="-50" />
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="15"
                              Direction="270"
                              Opacity="0.3"
                              ShadowDepth="5"
                              Color="Black" />
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="IconText"
                       Grid.Column="0"
                       Margin="0,0,15,0"
                       VerticalAlignment="Center"
                       FontFamily="Segoe UI Symbol"
                       FontSize="24"
                       Foreground="White"
                       Text="✓" />
            <TextBlock x:Name="MessageText"
                       Grid.Column="1"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="White"
                       TextWrapping="Wrap" />
        </Grid>
    </Border>
</UserControl>
