<Window x:Class="HRB.Platform.Client.WPF.Platform.Views.WindowShell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:core="clr-namespace:HRB.Payment.Core;assembly=HRB.Payment.Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        Width="1920"
        Height="1080"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Grid>

        <Grid>

            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ZIndex0Region}" />

            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}" />


            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.TaskNotifyRegion}" />

            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.DialogRegion}" />
            <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.LoadingRegion}" />

        </Grid>

    </Grid>

</Window>
