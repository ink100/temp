<Window x:Class="HRB.Payment.KeyTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HRB.Payment.KeyTool"
        mc:Ignorable="d"
        Title="MainWindow"
        MinHeight="768"
        MinWidth="1024"
        Height="768"
        Width="1024"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <TextBlock FontSize="14"
                       VerticalAlignment="Center">Client Key:</TextBlock>
            <TextBox x:Name="tbxClientKey"
                     Margin="20,0"
                     Width="300"
                     Height="35"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center" />

            <TextBlock FontSize="14"
                       VerticalAlignment="Center">有效期天数:</TextBlock>
            <TextBox x:Name="tbxValidDays"
                     Margin="20,0"
                     Height="35"
                     Width="80"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     Text="7" />

            <Button x:Name="btnCreateKeyFile"
                    FontSize="14"
                    Width="60"
                    Click="BtnCreateKeyFile_OnClick">生成</Button>

        </StackPanel>


        <TextBox Grid.Row="1"
                 x:Name="tbxLog"
                 Margin="0,20,0,0"
                 HorizontalScrollBarVisibility="Disabled"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch"
                 HorizontalContentAlignment="Left"
                 VerticalContentAlignment="Top"
                 Padding="10"
                 IsReadOnly="True" />


    </Grid>

</Window>
